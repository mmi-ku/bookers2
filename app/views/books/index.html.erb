<h1>User info</h1>
  <%= form_with model: @user do |f| %>
    <h4>name</h4>
    <%= f.text_field :name %>
    <h4>introduction</h4>
    <%= f.text_area :introduction %>
  <% end %>
  これ定義しなくてよい？current userになっているから、以下は消してよいのか。？？？？？？？
   <% if @user1 == current_user %>
    <p><%= link_to "編集の人ボタン追加する", edit_user_path(@user) %></p>
  <% end %>

<h1>New book</h1>
  <%= form_with model: @book do |f| %>
    <h4>Title</h4>
    <%= f.text_field :book_title %>
    <h4>Opinion</h4>
    <%= f.text_area :book_review %>
    <%= f.submit 'Create Book' %>
  <% end %>

<h1>Books</h1>

    <% @books.each do |book| %>
  <% end %>

